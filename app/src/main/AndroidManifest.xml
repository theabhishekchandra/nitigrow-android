<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Avatars/media are picked via the permission-less Android Photo Picker
         (PickVisualMedia). The Razorpay checkout SDK injects CAMERA / RECORD_AUDIO /
         READ_MEDIA_* for optional features NitiGrow doesn't use (and online payments
         are gated off in v1), so strip them here — otherwise Google Play requires
         photo/video + camera + mic permission declarations for features that aren't
         built. Re-add them (with the declarations) when those features actually ship. -->
    <uses-permission android:name="android.permission.CAMERA" tools:node="remove" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" tools:node="remove" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" tools:node="remove" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" tools:node="remove" />
    <!-- firebase-analytics injects the advertising-ID permission, but NitiGrow doesn't
         use the advertising ID (no ads, no cross-app tracking). Strip it so the Play
         "Advertising ID = No" and Data Safety declarations are accurate. Analytics
         still works without the ad ID. -->
    <uses-permission android:name="com.google.android.gms.permission.AD_ID" tools:node="remove" />

    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:name=".NitiGrowApplication"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="false"
        android:theme="@style/Theme.NitiGrow"
        tools:targetApi="36">

        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.NitiGrow">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter android:autoVerify="false">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="nitigrow" />
            </intent-filter>
            <!-- App Links: needs assetlinks.json on app.nitigrow.in/.well-known/ -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="app.nitigrow.in" />
                <data android:pathPrefix="/chat/" />
                <data android:pathPrefix="/campaign/" />
            </intent-filter>
        </activity>

        <service
            android:name=".core.notifications.NitiGrowMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- FCM notification quick-action receiver (Reply / Mark as read inline buttons). -->
        <receiver
            android:name=".core.notifications.QuickReplyReceiver"
            android:exported="false" />

        <!-- Home-screen widgets (Glance). -->
        <receiver
            android:name=".widget.QuickBroadcastReceiver"
            android:exported="true"
            android:label="@string/widget_quick_broadcast_title">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/quick_broadcast_widget_info" />
        </receiver>
        <receiver
            android:name=".widget.FestivalSendReceiver"
            android:exported="true"
            android:label="@string/widget_festival_send_title">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/festival_send_widget_info" />
        </receiver>

        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="chat" />

        <!--
          Firebase auto-init flags. Toggled per-build via manifest placeholders so
          debug builds with the stub google-services.json don't crash on Firebase
          API-key validation. Production build types flip these on once a real
          google-services.json is in place.
        -->
        <meta-data
            android:name="firebase_crashlytics_collection_enabled"
            android:value="${firebaseCrashlyticsEnabled}" />
        <meta-data
            android:name="firebase_performance_collection_enabled"
            android:value="${firebasePerfEnabled}" />
        <meta-data
            android:name="firebase_analytics_collection_enabled"
            android:value="${firebaseAnalyticsEnabled}" />
        <meta-data
            android:name="firebase_messaging_auto_init_enabled"
            android:value="${firebaseMessagingAutoInit}" />
    </application>

</manifest>
